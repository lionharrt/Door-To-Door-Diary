// import axios from "axios"

// const fetchLogin = () => axios.get("http://localhost:3000/user")

// const checkLogin = (userName, password) => {
//   fetchLogin()
//     .then(loginInfo => {
//       return userName === loginInfo.data[0].userName && password === loginInfo.data[0].password
//     })
//     .catch(error => console.log(error))
// }
// export default checkLogin

// FOR CHECKING 2 STRINGS

// for (let i = 0; i < password.length; i++) {
//         console.log(`char  ${i} :  ${password.charCodeAt(i)}`)
//       }
//       for (let i = 0; i < loginInfo.data[0].password.length; i++) {
//         console.log(`char  ${i} :  ${loginInfo.data[0].password.charCodeAt(i)}`)
//       }
